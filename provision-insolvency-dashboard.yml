---

- hosts: ansible_runner_boxes
  connection: local
  gather_facts: no
  tasks:
    - name: Provision insolvency dashboards
      include_role:
        name: grafana
      tags:
        - provision-insolvency-dashboard
